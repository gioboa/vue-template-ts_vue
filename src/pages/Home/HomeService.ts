import { Endpoint } from '../../../api/apiConsumer'
import axios from 'axios'

export default class HomeViewService {
  /*  public static storePage(data: {
    idUser: string;
    slug: string;
    idTemplate: string;
  }) {
    return axios.post(Endpoint.example.x.url, data);
  } */
  /* public static readAllModelos() {
    return axios.get(Endpoint.example.x.url);
  } */
  /* public static readAllModelosById(id) {
        return axios.get(Endpoint.preview.modelos.url, {
            id,
        });
    } */
}
